#include <bits/stdc++.h>
using namespace std;

bool checkIPaddress(string str)
{
	char_array[str.length() + 1];
	strcpy(char_array, str.c_str());
	int n = sizeof(char_array) / sizeof(char_array[0]);
	for (int i = 0; i < n; i++)
	{
		
	}
	return false;
}

int main()
{
	string str;
	getline(cin, str);
	cout << checkIPaddress(str);
	return 0;
}
