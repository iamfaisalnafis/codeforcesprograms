#include <iostream>
#include <string>

using namespace std;

void patsearchRabinKarp(string text, string pattern)
{

}

int main()
{
    string text;
    string pattern;
    cout << "Enter the text: ";
    getline(cin, text);
    cout << endl;
    cout << "Enter the pattern: ";
    getline(cin, pattern);
    patsearchRabinKarp(text, pattern);
    return 0;
}
